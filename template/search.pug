extends layout.pug

block scripts
    script(src="/public/resources/client-search.js")
    script.
      window.addEventListener(
        'load',
        function() {search.load("#{query}");},
        false
      );

block content
  include navigation-bar.pug

  h1 Results
  
  include search-bar.pug

  block results
    include search-results.pug
